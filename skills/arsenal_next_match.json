{
  "id": "arsenal_next_match",
  "name": "Arsenal Next Match",
  "description": "Shows Arsenal's next scheduled match on demand via /arsenal command.",
  "trigger": {
    "type": "command",
    "command": "arsenal"
  },
  "data_sources": [
    {
      "id": "match",
      "type": "http",
      "url": "https://api.football-data.org/v4/teams/57/matches",
      "method": "GET",
      "params": {
        "status": "SCHEDULED",
        "limit": 1
      },
      "headers": {
        "X-Auth-Token": "{{FOOTBALL_API_KEY}}"
      },
      "body": null,
      "timeout": 15
    }
  ],
  "message_template": "⚽ *Arsenal — Next Match*\n\n{% if match.matches %}{% set m = match.matches[0] %}🏆 _{{ m.competition.name }}_\n\n🔴 *{{ m.homeTeam.name }}* vs *{{ m.awayTeam.name }}*\n\n📅 {{ m.utcDate | format_date('%A, %b %d') }}\n🕐 {{ m.utcDate | relative_time }}{% else %}No upcoming matches found.{% endif %}",
  "parse_mode": "Markdown",
  "enabled": true,
  "created_at": "2026-03-14T18:38:19.123905"
}